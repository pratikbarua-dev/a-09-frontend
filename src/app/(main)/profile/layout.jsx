export const metadata = {
  title: "My Profile",
  description:
    "View and update your personal information, account settings, and health overview on DocAppoint.",
};

export default function ProfileLayout({ children }) {
  return children;
}
